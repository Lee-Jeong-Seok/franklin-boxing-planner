# RULES.md - 프랭클린 타임박싱 플래너 개발 및 보안 규칙

이 문서는 개발 과정에서 반드시 준수해야 하는 코딩 컨벤션, 보안 요구사항, 수익화 정책 제약조건 및 시스템 규칙을 정의합니다.

---

## 🔒 1. 보안 및 개인정보 보호 규칙 (Security & Privacy Rules)

1. **클라이언트 암호화 원칙 (Web Crypto API)**:
   * 사용자의 일일 할 일, 타임박스 내용, 메모 등 모든 플래너 데이터는 저장(IndexedDB)되거나 내보내기(JSON 백업)되기 전에 **AES-GCM 알고리즘**으로 암호화되어야 합니다.
   * 사용자의 PIN 번호는 마스터 키 생성을 위한 재료로만 사용되며, 평문으로 스토리지에 저장되거나 외부로 유출되어서는 안 됩니다.
2. **서버 최소 데이터 원칙**:
   * Firebase Firestore 등 클라우드 데이터베이스에는 유저의 개인 플래너 내용이 절대 저장되지 않아야 합니다.
   * Firestore에 저장 가능한 데이터는 오직 **라이선스 관리 목적의 메타데이터**(라이선스 키, 발급/사용 상태, 기기 고유 UUID)로 제한됩니다.

---

## 🏗️ 2. 아키텍처 및 코딩 규칙 (Architecture Rules)

1. **Zero-Server-Cost 유지**:
   * 고가의 백엔드 서버 DB나 유료 API 서버 구축 없이 정적 호스팅(Firebase Hosting)과 클라이언트 측 컴퓨팅 자원만을 활용합니다.
2. **경량화 및 의존성 최소화**:
   * 무거운 프레임워크나 불필요한 라이브러리 도입을 지양하고, 브라우저 표준 API(IndexedDB, Web Crypto API, Vanilla JS, HTML5)를 최우선적으로 활용합니다.
3. **오류 처리 및 예외 안전성**:
   * 복호화 실패(잘못된 PIN 입력), DB 읽기/쓰기 실패, 네트워크 미연결 시 애플리케이션이 크래시되지 않고 적절한 안내 메시지(UI alert/modal)를 제공해야 합니다.

---

## 💳 3. 요금제 및 기능 제약 규칙 (Tier Policy Rules)

| 항목 | 🟢 Free (무료) | 🔵 Essential (4,900원 1회 결제) | 🟣 Unlimited (월 3,900원 구독) |
| :--- | :--- | :--- | :--- |
| **기본 플래너/암호화** | 제공 (제한 없음) | 제공 (제한 없음) | 제공 (제한 없음) |
| **수동 JSON 백업** | **하루 1회 제한** | **무제한** | **무제한** |
| **통계 대시보드** | 최근 **7일**만 조회 가능 | 최근 **30일**까지 조회 가능 | 최근 **90일 / 전체** 조회 가능 |
| **목표/사명 관리** | 접근 불가 (Unlimited 전용) | 접근 불가 (Unlimited 전용) | **전용 작성 및 보관 기능 제공** |
| **클라우드 동기화** | 미제공 | 미제공 | **구글 드라이브 OAuth 연동 동기화** |

* **Essential 라이선스 검증 규칙**:
  * 라이선스 인증 시 Firebase Firestore와의 실시간 조회 및 클라이언트 UUID 등록이 이루어져야 합니다.
  * 이미 다른 `deviceId`에 사용 처리된 키는 중복 등록을 차단해야 합니다.

---

## 📁 4. 프로젝트 및 문서 관리 규칙 (Project Management Rules)

1. **디렉터리 구조 통일**:
   * 루트 디렉터리에 임의의 일자별 진행 내용 폴더(예: `2026-06-09 진행 내용`)를 개별적으로 생성하지 않습니다.
   * 개발 이력은 반드시 `history/` 폴더 내에 일자별 Markdown 파일(`YYYY-MM-DD.md`)로 기록합니다.
2. **소스 코드 반영 규칙**:
   * 배포 대상인 `public/index.html`을 항상 최신 상태로 유지하며, 백업용 정적 HTML 업데이트 시 정합성을 확인합니다.
